# Copy to keystore.properties (same folder) if you regenerate credentials locally.
# Paths are relative to android/signing/.
storeFile=aile-release.jks
storePassword=REPLACE_ME
keyAlias=aile
keyPassword=REPLACE_ME
